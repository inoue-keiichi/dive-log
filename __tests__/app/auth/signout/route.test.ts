import { GET } from "@/app/auth/callback/route";
import { SITE_URL } from "@/utils/commons";
import { NextRequest } from "next/server";

describe("POST API", () => {
  it("succeeds", async () => {
    // NEXT_RSC_ERR_CLIENT_IMPORT: next/headers というエラーが出る
    // nextjs のバージョンアップで解消できそうなので保留
  });
});
