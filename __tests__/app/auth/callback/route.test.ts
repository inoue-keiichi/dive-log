describe("GET API", () => {
  it("succeeds", async () => {
    // NEXT_RSC_ERR_CLIENT_IMPORT: next/headers というエラーが出る
    // nextjs のバージョンアップで解消できそうなので保留
    // GET(new NextRequest(new URL(`${SITE_URL}/auth/callback`)));
  });
});
